%h1
  = @post.title
= form_with model: @post, url: user_edit_post_path(current_user.name,@post.title,@post.number) ,local:true do |form|
  %div
    .content
      = form.rich_text_area :content, :value => @post.content
  = form.submit '投稿', class: 'btn-square'